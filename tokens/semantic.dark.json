{
  "$description": "Semantic roles, dark theme. This is the layer application code is allowed to use. A role names the job a value does, so re-theming means re-pointing these and nothing else.",

  "surface": {
    "$type": "color",
    "$description": "Two-pole ladder: the well behind cards is the darkest plane, chrome sits one step up, and raised content reads brighter than the void behind it.",
    "page":    { "$value": "{ink.0}", "$description": "Body, the content well behind cards" },
    "chrome":  { "$value": "{ink.1}", "$description": "Topbar and sidenav" },
    "card":    { "$value": "{ink.2}", "$description": "Cards, tables, panels" },
    "inset":   { "$value": "{ink.3}", "$description": "Inputs, secondary buttons" },
    "raised":  { "$value": "{ink.3}", "$description": "Modals, dropdowns, popovers" },
    "hover":   { "$value": "{ink.4}", "$description": "Interactive hover and tertiary fills" },
    "inert":   { "$value": "{ink.5}", "$description": "Chart grid, track fills, skeleton states" },
    "subtle":  { "$value": "rgba(255, 255, 255, 0.05)", "$description": "Tint wash over an unknown surface: nav items, chips, zebra rows" },
    "inverse": { "$value": "{ink.10}", "$description": "Inverted surfaces: tooltips" },
    "paper":   { "$value": "{paper}", "$description": "Brand cream insert" }
  },

  "content": {
    "$type": "color",
    "$description": "Text and icons, loudest to quietest. Every role except disabled meets WCAG AA on every surface in the ladder.",
    "emphasis":  { "$value": "{ink.10}", "$description": "Headings, active nav, key figures" },
    "default":   { "$value": "{ink.9}",  "$description": "Body copy, table cells" },
    "secondary": { "$value": "{ink.8}",  "$description": "Labels, eyebrows, column headers" },
    "muted":     { "$value": "{ink.7}",  "$description": "Hints, placeholders, timestamps" },
    "disabled":  { "$value": "{ink.6}",  "$description": "Disabled controls. Exempt from AA per WCAG 1.4.3, and never the only signal that a control is off." },
    "inverse":   { "$value": "{ink.1}",  "$description": "Text on surface-inverse" },
    "on-accent": { "$value": "{white}",  "$description": "Text on an accent fill" }
  },

  "border": {
    "$type": "color",
    "$description": "Hairlines, not weight. Translucent so they read correctly over any surface in the ladder.",
    "subtle":  { "$value": "rgba(255, 255, 255, 0.06)", "$description": "Internal dividers, table rows" },
    "default": { "$value": "rgba(255, 255, 255, 0.08)", "$description": "Card and input edges, the default" },
    "strong":  { "$value": "rgba(255, 255, 255, 0.14)", "$description": "Hover edges, emphasis" },
    "accent":  { "$value": "{red.3}", "$description": "Active tab underline, focused input, selected row rail" }
  },

  "accent": {
    "$type": "color",
    "$description": "One saturated hue in the whole system. Sparing use is what makes it read as signal. The fill carries a white label at AA; text has its own tinted step because the fill reaches only 3.85:1 on surface-card.",
    "default":  { "$value": "{red.3}", "$description": "Accent icons, borders, active nav indicators" },
    "hover":    { "$value": "{red.2}", "$description": "Hover for accent icons and links" },
    "text":     { "$value": "{red.1}", "$description": "Accent-coloured text and links, and the active nav marker bar. Tinted because the product accent reaches only 3.85:1 on surface-card." },
    "fill":     { "$value": "{red.3}", "$description": "Primary button background. White on it is 4.50:1." },
    "fill-hover":  { "$value": "rgba(225, 45, 70, 0.9)", "$description": "Primary button hover: the fill at 90%, so it lightens slightly over its backdrop. White on it is 4.0:1 over light surfaces, a documented exception, and 5.2:1 over dark ones." },
    "fill-active": { "$value": "{red.7}", "$description": "Primary button pressed" },
    "subtle":   { "$value": "rgba(228, 142, 150, 0.20)", "$description": "Active nav item pill, selected row: accent text at 20%" },
    "gradient":      { "$type": "gradient", "$value": "linear-gradient(90deg, {gradient.display.1} 0%, {gradient.display.2} 51%, {gradient.display.3} 100%)", "$description": "The logo gradient. Marks and rules only, nothing on top of it." },
    "gradient-fill": { "$type": "gradient", "$value": "linear-gradient(270deg, {gradient.display.3} 0%, {red.3} 30%, {red.3} 70%, {gradient.display.1} 100%)", "$description": "Banner gradient: the logo's end colours with the accent fill held across the middle, where the text sits. White text only on the solid band." }
  },

  "status": {
    "$type": "color",
    "$description": "Eight roles per state. `default` is the hue as a graphic (dot, icon, chart series) and must reach 3:1 on a card. `text` is the label on a normal surface at 4.5:1. `fill` is a solid badge or button background with `on-fill` as its label, and on dark it is also the 3px edge on large status surfaces. `subtle` and `subtle-border` are a status chip: `text` on a pastel with a faint edge of the same hue. `surface` and `border` are a large status surface (callout, banner, tile). On dark those are neutral, and the state is carried by a 3px edge in `fill`, because a tinted dark surface cannot get light enough without costing the text on it contrast. Body text on a large surface stays content-default; only the icon and title take `text`.",
    "success": {
      "default": { "$value": "{green.4}" },
      "text":    { "$value": "{green.4}" },
      "fill":    { "$value": "{green.5}" },
      "on-fill": { "$value": "{white}" },
      "subtle":  { "$value": "{green.8}" },
      "subtle-border": { "$value": "{green.7}" },
      "surface": { "$value": "{surface.card}" },
      "border":  { "$value": "{surface.inert}" }
    },
    "warning": {
      "default": { "$value": "{amber.5}" },
      "text":    { "$value": "{amber.5}" },
      "fill":    { "$value": "{amber.4}" },
      "on-fill": { "$value": "{cream.10}", "$description": "Dark label. The warning fill is too light for white." },
      "subtle":  { "$value": "{amber.8}" },
      "subtle-border": { "$value": "{amber.7}" },
      "surface": { "$value": "{surface.card}" },
      "border":  { "$value": "{surface.inert}" }
    },
    "danger": {
      "default": { "$value": "{scarlet.4}", "$description": "A true red, separate from the crimson accent." },
      "text":    { "$value": "{scarlet.4}" },
      "fill":    { "$value": "{scarlet.5}" },
      "on-fill": { "$value": "{white}" },
      "subtle":  { "$value": "{scarlet.8}" },
      "subtle-border": { "$value": "{scarlet.7}" },
      "surface": { "$value": "{surface.card}" },
      "border":  { "$value": "{surface.inert}" }
    },
    "info": {
      "default": { "$value": "{stone.1}", "$description": "Neutral grey. The brand has no blue." },
      "text":    { "$value": "{stone.1}" },
      "fill":    { "$value": "{stone.2}" },
      "on-fill": { "$value": "{white}" },
      "subtle":  { "$value": "{surface.hover}", "$description": "Neutral chips are the hover grey, with no edge." },
      "subtle-border": { "$value": "rgba(0, 0, 0, 0)" },
      "surface": { "$value": "{surface.card}", "$description": "Info is neutral in both themes and takes no edge." },
      "border":  { "$value": "{surface.inert}" }
    }
  },

  "shadow": {
    "$type": "shadow",
    "$description": "Deeper on near-black surfaces than they would be on light, because elevation cannot be read from brightness alone here.",
    "card":     { "$value": "0 1px 3px rgba(0, 0, 0, 0.5), 0 0 0 1px rgba(255, 255, 255, 0.04)" },
    "dropdown": { "$value": "0 4px 16px rgba(0, 0, 0, 0.5)" },
    "modal":    { "$value": "0 8px 32px rgba(0, 0, 0, 0.6)" },
    "focus":    { "$value": "0 0 0 3px rgba(224, 75, 69, 0.35)", "$description": "Focus ring. Never removed without a replacement." }
  }
}
